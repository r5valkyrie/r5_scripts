# This directory is used for updating base files during development for contributors of the scripts repo
# The files in this directory need moved to their root locations ( if newer ).

Copy all folders/files to main R5Reloaded directory (where r5apex.exe resides)
